on: [push]

name: CI

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: cargo build --profile ci
      - uses: actions/upload-artifact@v4
        with:
          name: razer-sdk-reader-rs.exe
          path: target/ci/razer-sdk-reader-rs.exe
